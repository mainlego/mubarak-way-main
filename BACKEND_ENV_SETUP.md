# 🔧 Backend Environment Variables Setup

## Проблема: AI ассистент не работает

**Ошибка:** `AI service is not configured`

**Причина:** На Render.com не настроен API ключ для OpenAI.

---

## ✅ Решение: Добавить OPENAI_API_KEY

### Шаг 1: Получить OpenAI API Key

1. Перейдите на [https://platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Войдите в свой аккаунт OpenAI (или создайте новый)
3. Нажмите **"Create new secret key"**
4. Скопируйте ключ (формат: `sk-proj-...`)
5. **ВАЖНО:** Сохраните ключ в безопасном месте - он больше не будет показан

### Шаг 2: Добавить ключ в Render

1. Перейдите на [https://dashboard.render.com](https://dashboard.render.com)
2. Найдите ваш backend сервис: **mubarak-way-backend**
3. Перейдите в **Environment** (в левом меню)
4. Нажмите **"Add Environment Variable"**
5. Заполните:
   - **Key:** `OPENAI_API_KEY`
   - **Value:** `sk-proj-...` (ваш ключ из шага 1)
6. Нажмите **"Save Changes"**

### Шаг 3: Перезапустить сервис

1. После сохранения Render автоматически перезапустит backend
2. Подождите 1-2 минуты, пока сервис перезапустится
3. Проверьте логи: должно появиться `✅ OpenAI client initialized`

---

## 📋 Полный список необходимых переменных окружения

Для работы backend нужны следующие переменные:

### Обязательные (Required):

```bash
# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mubarak-way

# Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this

# OpenAI (для AI ассистента)
OPENAI_API_KEY=sk-proj-...
```

### Опциональные (Optional):

```bash
# Node Environment
NODE_ENV=production

# Server
PORT=4000
ALLOWED_ORIGINS=https://your-frontend-url.onrender.com

# Elasticsearch (для расширенного поиска)
ELASTICSEARCH_API_URL=https://bot.e-replika.ru
ELASTICSEARCH_API_TOKEN=your-token

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_AI_MAX_REQUESTS=10

# Cache
CACHE_TTL=3600
```

---

## 🔍 Проверка конфигурации

После добавления `OPENAI_API_KEY`, проверьте работу AI:

### Тест через curl:

```bash
curl -X POST https://mubarak-way-backend.onrender.com/api/v1/ai/ask \
  -H "Content-Type: application/json" \
  -d '{"question": "Что такое Ислам?", "language": "ru"}'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "data": {
    "question": "Что такое Ислам?",
    "answer": {
      "answer": "🤖 **Ответ:**\n\n## Ислам...",
      "sources": [],
      "relatedVerses": []
    }
  }
}
```

### Тест в приложении:

1. Откройте [https://mubarak-way-frontend-zibs.onrender.com/ai](https://mubarak-way-frontend-zibs.onrender.com/ai)
2. Задайте вопрос: "Что такое Ислам?"
3. Должен прийти ответ от AI ассистента

---

## ⚠️ Важные замечания

### Стоимость OpenAI API

- **Модель:** `gpt-4o-mini` (самая дешевая от OpenAI)
- **Цена:** ~$0.15 за 1M входных токенов, ~$0.60 за 1M выходных токенов
- **Средний запрос:** ~500 входных + 800 выходных токенов = ~$0.00065 (меньше цента)
- **100 запросов:** ~$0.065 (6.5 центов)

### Лимиты бесплатного тира OpenAI

- **Free tier:** $5 кредитов (истекает через 3 месяца после регистрации)
- **Примерно:** 7,500 AI запросов бесплатно
- **После истечения:** нужно добавить платежный метод

### Альтернативы (если нужно сэкономить)

1. **Anthropic Claude** (уже есть в коде):
   - Установите `ANTHROPIC_API_KEY` вместо `OPENAI_API_KEY`
   - Измените `AIService.ts` чтобы использовать Claude SDK
   - Примерно та же цена, но лучшее качество

2. **Бесплатные альтернативы:**
   - Ollama (локально запущенная модель)
   - Hugging Face Inference API (бесплатные модели)
   - Google Gemini API (щедрый free tier)

---

## 🐛 Troubleshooting

### Ошибка: "AI service is not configured"

**Причина:** `OPENAI_API_KEY` не установлен или пустой.

**Решение:**
1. Проверьте Environment Variables на Render
2. Убедитесь, что ключ начинается с `sk-proj-` или `sk-`
3. Перезапустите сервис после добавления ключа

### Ошибка: "Incorrect API key provided"

**Причина:** Неверный или истекший API ключ.

**Решение:**
1. Создайте новый ключ на [platform.openai.com](https://platform.openai.com/api-keys)
2. Обновите `OPENAI_API_KEY` на Render
3. Перезапустите сервис

### Ошибка: "You exceeded your current quota"

**Причина:** Закончились бесплатные кредиты OpenAI.

**Решение:**
1. Добавьте платежный метод на [platform.openai.com/account/billing](https://platform.openai.com/account/billing)
2. Или используйте альтернативный AI провайдер

### Backend все еще спит (502 Bad Gateway)

**Причина:** Render free tier усыпляет сервис после 15 минут неактивности.

**Решение:**
- Подождите 30-50 секунд после первого запроса
- Frontend уже показывает пользователю сообщение об этом
- Для постоянной работы: upgrade to paid tier ($7/month)

---

## 📞 Поддержка

Если проблема не решилась:

1. Проверьте логи на Render Dashboard
2. Посмотрите Console в браузере (F12)
3. Проверьте статус backend: `curl https://mubarak-way-backend.onrender.com/api/v1/version`

---

## ✅ Checklist после настройки

- [ ] `OPENAI_API_KEY` добавлен в Render Environment Variables
- [ ] Backend перезапущен (автоматически после сохранения)
- [ ] Логи показывают успешный старт (без ошибок)
- [ ] Тест curl возвращает success
- [ ] AI ассистент работает в приложении
- [ ] Вопросы получают ответы от AI

---

**Время настройки:** 5 минут
**Сложность:** Легко
**Статус после настройки:** AI ассистент полностью работает ✅
