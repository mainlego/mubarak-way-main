# Автоматическое версионирование

## 🎯 Обзор

Проект использует автоматическую систему версионирования на основе [Semantic Versioning](https://semver.org/lang/ru/):
- **MAJOR** (1.0.0 → 2.0.0) - Breaking changes
- **MINOR** (1.0.0 → 1.1.0) - Новые функции
- **PATCH** (1.0.0 → 1.0.1) - Исправления багов

## 🚀 Как это работает

### Автоматическое определение версии по коммиту

Версия автоматически увеличивается на основе **первого слова** в сообщении коммита:

| Ключевое слово | Тип изменения | Пример | Результат |
|---------------|---------------|--------|-----------|
| `feat:` | Minor | `feat: add audio player` | 1.0.0 → 1.1.0 |
| `fix:` | Patch | `fix: resolve login bug` | 1.0.0 → 1.0.1 |
| `BREAKING CHANGE:` | Major | `BREAKING CHANGE: API v2` | 1.0.0 → 2.0.0 |

### Примеры коммитов

```bash
# Патч версия (1.0.0 → 1.0.1)
git commit -m "fix: исправлена ошибка загрузки нашидов"

# Минорная версия (1.0.0 → 1.1.0)
git commit -m "feat: добавлен аудиоплеер"

# Мажорная версия (1.0.0 → 2.0.0)
git commit -m "BREAKING CHANGE: новая система авторизации"
```

## 📦 Установка

### 1. Установка Git hooks (один раз)

```bash
# Автоматически при npm install
npm install

# Или вручную
npm run version:setup
```

### 2. Проверка установки

```bash
# Должен существовать файл
ls -la .git/hooks/post-commit
```

## 🔄 Процесс версионирования

При каждом коммите автоматически:

1. ✅ Определяется тип изменения (major/minor/patch)
2. ✅ Увеличивается версия во всех `package.json`
3. ✅ Обновляется `frontend/src/shared/config/version.ts`
4. ✅ Добавляется запись в `CHANGELOG.md`
5. ✅ Изменения добавляются в тот же коммит (`git commit --amend`)

## 📝 Структура коммит-сообщений

### Рекомендуемый формат

```
<type>: <краткое описание>

<подробное описание (опционально)>

<footer (опционально)>
```

### Типы коммитов

- `feat:` - Новая функция
- `fix:` - Исправление бага
- `docs:` - Изменения в документации
- `style:` - Форматирование кода
- `refactor:` - Рефакторинг кода
- `test:` - Добавление тестов
- `chore:` - Рутинные задачи (build, CI, etc.)
- `perf:` - Оптимизация производительности

### Примеры хороших коммитов

```bash
# ✅ Хорошо
git commit -m "feat: add GlobalAudioPlayer component"
git commit -m "fix: resolve category filtering in BooksPage"
git commit -m "docs: update API documentation"

# ❌ Плохо (версия не увеличится правильно)
git commit -m "обновление"
git commit -m "фиксы"
git commit -m "WIP"
```

## 🎨 CHANGELOG.md

Автоматически генерируется с каждым коммитом:

```markdown
## [1.2.3] - 2025-11-04

### ✨ New Feature

feat: add GlobalAudioPlayer component

- Full audio player integration
- Minimized and full-screen modes
- Volume control and progress bar

---

## [1.2.2] - 2025-11-03

### 🐛 Bug Fix

fix: resolve category filtering in BooksPage

---
```

## 🔧 Ручное управление версией

### Bump версии вручную

```bash
# Запустить скрипт версионирования
npm run version:bump

# Или напрямую
node scripts/bump-version.js
```

### Отключить автоматическое версионирование

```bash
# Временно (для одного коммита)
git commit --no-verify -m "сообщение"

# Постоянно (удалить хук)
rm .git/hooks/post-commit
```

### Восстановить хук

```bash
npm run version:setup
```

## 📊 Проверка текущей версии

### Frontend

```typescript
// В коде
import { APP_VERSION } from '@shared/config/version';
console.log(APP_VERSION); // "1.2.3"
```

### Backend

```bash
# API endpoint
curl https://your-api.com/api/v1/version

# Response
{
  "success": true,
  "version": "1.2.3",
  "timestamp": "2025-11-04T12:00:00.000Z",
  "environment": "production"
}
```

### Package.json

```bash
# Все версии должны совпадать
cat frontend/package.json | grep version
cat backend/package.json | grep version
cat shared/package.json | grep version
```

## 🚀 Деплой с новой версией

```bash
# 1. Коммит с описанием изменений
git commit -m "feat: add new dashboard widget"
# → Автоматически версия 1.0.0 → 1.1.0

# 2. Push в репозиторий
git push origin main

# 3. На сервере автоматически:
#    - Render деплоит новую версию
#    - Пользователи получают уведомление через 5 минут
```

## 🔍 Диагностика проблем

### Хук не срабатывает

```bash
# 1. Проверить права
ls -la .git/hooks/post-commit
chmod +x .git/hooks/post-commit

# 2. Переустановить хук
npm run version:setup

# 3. Проверить скрипт
node scripts/bump-version.js
```

### Версия не обновляется

```bash
# 1. Проверить формат коммита
git log -1 --pretty=%B

# 2. Должен начинаться с feat:, fix:, etc.
# 3. Попробовать вручную
npm run version:bump
```

### Разные версии в package.json

```bash
# Синхронизировать версии
# Скрипт обновит все package.json одновременно
npm run version:bump
```

## 🎯 Best Practices

### ✅ DO

- Используйте английский язык в коммитах
- Указывайте тип изменения (`feat:`, `fix:`, etc.)
- Пишите понятные описания
- Коммитьте логически связанные изменения
- Проверяйте версию перед push

### ❌ DON'T

- Не используйте generic сообщения ("update", "fix")
- Не смешивайте несколько типов изменений в одном коммите
- Не забывайте про prefix (`feat:`, `fix:`)
- Не используйте `--no-verify` без необходимости

## 📚 Дополнительные ресурсы

- [Semantic Versioning](https://semver.org/lang/ru/)
- [Conventional Commits](https://www.conventionalcommits.org/ru/v1.0.0/)
- [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/)

## 🆘 Поддержка

Если возникли проблемы с версионированием:

1. Проверьте этот документ
2. Запустите `npm run version:setup`
3. Проверьте права на `.git/hooks/post-commit`
4. Создайте issue с описанием проблемы

---

**Автоматическое версионирование настроено и готово к использованию! 🎉**
