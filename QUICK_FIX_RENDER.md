# 🚀 Быстрое исправление AI на Render

## ⚡ Проблема

```
timeout of 30000ms exceeded
```

AI не отвечает в течение 30 секунд. Сервер на Render не обрабатывает запросы.

## ✅ Решение (3 минуты)

### Шаг 1: Зайдите на Render Dashboard

1. Откройте https://dashboard.render.com/
2. Найдите сервис **mubarak-way-backend**
3. Перейдите в раздел **Environment**

### Шаг 2: Добавьте OPENAI_API_KEY

⚠️ **КРИТИЧНО!** Без этого AI не работает!

1. Откройте ваш локальный файл `backend/.env`
2. Найдите строку:
   ```
   OPENAI_API_KEY=sk-proj-...
   ```
3. Скопируйте **весь ключ** (начинается с `sk-proj-`)
4. На Render нажмите **Add Environment Variable**
5. Введите:
   - **Key**: `OPENAI_API_KEY`
   - **Value**: `sk-proj-...` (вставьте скопированный ключ)
6. Нажмите **Save**

### Шаг 3: Проверьте другие переменные

Убедитесь что также добавлены:

- ✅ `MONGODB_URI` (из локального `.env`)
- ✅ `TELEGRAM_BOT_TOKEN` (из локального `.env`)
- ✅ `JWT_SECRET=mubarakway-admin-jwt-secret-2025-production-key`
- ✅ `NODE_ENV=production`
- ✅ `ALLOWED_ORIGINS=https://mubarak-way-frontend.onrender.com,https://web.telegram.org`

### Шаг 4: Дождитесь деплоя

После сохранения переменных:
1. Render автоматически начнёт **re-deploy**
2. Следите за **Logs** (вкладка)
3. Дождитесь строк:
   ```
   ✅ MongoDB connected successfully
   🚀 Server listening on port 10000
   ```

### Шаг 5: Протестируйте

1. Откройте Telegram Mini App
2. Перейдите в **AI Ассистент**
3. Отправьте любой вопрос
4. Ответ должен прийти через 5-15 секунд

## 🔍 Если не работает

### Проверьте логи на Render

В **Logs** ищите ошибки:

❌ **Ошибка: OPENAI_API_KEY not configured**
```
AI ask error: Error: OPENAI_API_KEY not configured
```
→ Добавьте `OPENAI_API_KEY` в Environment

❌ **Ошибка: 401 Unauthorized**
```
Error: 401 Unauthorized - Invalid API key
```
→ API key неверный. Проверьте на https://platform.openai.com/api-keys

❌ **Ошибка: 429 Quota exceeded**
```
Error: 429 You exceeded your current quota
```
→ Закончились кредиты. Пополните на https://platform.openai.com/billing

### Проверьте OpenAI Account

1. Зайдите на https://platform.openai.com/
2. **Billing** → **Usage**
3. Убедитесь:
   - ✅ Есть активная подписка или кредиты ($5-10 достаточно)
   - ✅ API key действителен
   - ✅ Лимит не превышен

## 📚 Полная документация

Смотрите [RENDER_AI_TROUBLESHOOTING.md](RENDER_AI_TROUBLESHOOTING.md) для:
- Детальной диагностики
- Решения всех возможных ошибок
- Debug checklist
- Рекомендаций по оптимизации

## ✅ Что было исправлено в коде

1. **Увеличен timeout с 30 до 60 секунд** - компенсирует холодный старт Render free tier
   - Файл: `frontend/src/shared/lib/api.ts:12`

2. **Добавлено детальное логирование** - помогает диагностировать проблемы
   - Файл: `frontend/src/pages/quran/AIChatPage.tsx`
   - Логи: 🤖 запрос, ✅ успех, ❌ ошибка, 📝 обработка

3. **Переименовано "AI Помощник" → "AI Ассистент"**
   - Файл: `frontend/src/shared/i18n/locales/ru.json:223`

## 🎯 Итог

**После добавления `OPENAI_API_KEY` на Render:**
- ✅ AI будет отвечать через 5-15 секунд
- ✅ Первый запрос может быть медленнее (cold start)
- ✅ Следующие запросы будут быстрее

**Если проблема сохраняется:**
1. Проверьте логи на Render
2. Убедитесь что переменные сохранены
3. Перезапустите сервис вручную (Manual Deploy)
4. Проверьте баланс на OpenAI

---

**Обновлено**: 2025-10-28
**Статус**: ✅ Готово
**Приоритет**: 🔴 Критично
