# Scripts Directory

## 📁 Содержимое

### `bump-version.js`
Автоматический скрипт для увеличения версии на основе коммит-сообщений.

**Использование:**
```bash
node scripts/bump-version.js
```

**Логика версионирования:**
- `feat:` → minor version (1.0.0 → 1.1.0)
- `fix:` → patch version (1.0.0 → 1.0.1)
- `BREAKING CHANGE:` → major version (1.0.0 → 2.0.0)

**Что делает:**
1. Читает последнее коммит-сообщение
2. Определяет тип изменения
3. Обновляет версию в:
   - `frontend/package.json`
   - `backend/package.json`
   - `shared/package.json`
   - `frontend/src/shared/config/version.ts`
4. Добавляет запись в `CHANGELOG.md`

---

### `setup-hooks.sh`
Установка Git hooks для автоматического версионирования.

**Использование:**
```bash
bash scripts/setup-hooks.sh
# или
npm run version:setup
```

**Что делает:**
- Создает `.git/hooks/post-commit` hook
- Настраивает автоматический запуск `bump-version.js` после каждого коммита
- Автоматически добавляет изменения версии в тот же коммит

---

## 🚀 Quick Start

### 1. Первая установка

```bash
# Автоматически при установке зависимостей
npm install

# Или вручную
npm run version:setup
```

### 2. Проверка установки

```bash
# Должен существовать и быть исполняемым
ls -la .git/hooks/post-commit
```

### 3. Первый коммит с автоверсионированием

```bash
git add .
git commit -m "feat: add new feature"
# → Версия автоматически увеличится 1.0.0 → 1.1.0

git push
```

---

## 📝 Примеры использования

### Добавление новой функции

```bash
git add .
git commit -m "feat: add audio player to nashids"
# Результат: 1.0.0 → 1.1.0
```

### Исправление бага

```bash
git add .
git commit -m "fix: resolve category filtering issue"
# Результат: 1.1.0 → 1.1.1
```

### Breaking change

```bash
git add .
git commit -m "BREAKING CHANGE: refactor API endpoints"
# Результат: 1.1.1 → 2.0.0
```

---

## 🔧 Ручной режим

### Bump версии без коммита

```bash
# Запустить скрипт вручную
npm run version:bump

# Или напрямую
node scripts/bump-version.js
```

### Отключить автоматическое версионирование

```bash
# Временно (один коммит)
git commit --no-verify -m "message"

# Постоянно (удалить хук)
rm .git/hooks/post-commit
```

### Восстановить хук

```bash
npm run version:setup
```

---

## 🐛 Troubleshooting

### Хук не срабатывает

**Проблема:** Версия не обновляется после коммита

**Решение:**
```bash
# 1. Проверить наличие хука
ls -la .git/hooks/post-commit

# 2. Переустановить
npm run version:setup

# 3. Проверить права
chmod +x .git/hooks/post-commit

# 4. Проверить скрипт вручную
node scripts/bump-version.js
```

### Неправильный тип версии

**Проблема:** Патч версия вместо минорной

**Причина:** Коммит-сообщение не начинается с `feat:`

**Решение:**
```bash
# ✅ Правильно
git commit -m "feat: new feature"

# ❌ Неправильно
git commit -m "new feature"
```

### Разные версии в package.json

**Проблема:** Frontend и Backend имеют разные версии

**Решение:**
```bash
# Скрипт синхронизирует все версии
npm run version:bump
```

---

## 📊 Структура версии

Проект использует [Semantic Versioning](https://semver.org/):

```
MAJOR.MINOR.PATCH
  │     │     │
  │     │     └─ Patch: Исправления багов (1.0.0 → 1.0.1)
  │     └─────── Minor: Новые функции (1.0.0 → 1.1.0)
  └───────────── Major: Breaking changes (1.0.0 → 2.0.0)
```

---

## 🔍 Проверка версии

### В коде (Frontend)

```typescript
import { APP_VERSION } from '@shared/config/version';
console.log(APP_VERSION); // "1.2.3"
```

### API (Backend)

```bash
curl https://your-api.com/api/v1/version
```

### Package.json

```bash
cat frontend/package.json | grep version
cat backend/package.json | grep version
cat shared/package.json | grep version
```

Все версии должны совпадать!

---

## 📚 Дополнительные материалы

- [VERSIONING.md](../VERSIONING.md) - Полная документация по версионированию
- [CHANGELOG.md](../CHANGELOG.md) - История изменений
- [Semantic Versioning](https://semver.org/lang/ru/)
- [Conventional Commits](https://www.conventionalcommits.org/ru/v1.0.0/)

---

**Скрипты готовы к использованию! 🎉**
